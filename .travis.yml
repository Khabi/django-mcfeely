language: python
python:
    - "2.7"
    - "2.6"

env:
    - DJANGO=1.4.3
    - DJANGO=1.3.5
    - DJANGO=1.2.7

install:
    - pip install -q Django==$DJANGO --use-mirrors

script: python setup.py test

notifications:
    email:
        recipients:
            - code@pixelatedninja.com